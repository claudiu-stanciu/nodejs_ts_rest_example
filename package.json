{
  "name": "nodejs_rest_tut",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsc && npm run build:live && concurrently \"tsc -w\" ",
    "build:live": "nodemon --exec ts-node -- ./index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "ts-node": "./node_modules/.bin/ts-node"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^3.1.0",
    "express": "^4.14.0",
    "lite-server": "^2.2.2",
    "nodemon": "^1.11.0",
    "ts-node": "^2.0.0",
    "typescript": "^2.1.4"
  },
  "repository": {
  "type": "git",
  "url": "git+https://github.com/claudiu-stanciu/nodejs_ts_rest_example.git"
  }
}
